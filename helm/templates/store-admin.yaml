apiVersion: apps/v1
kind: Deployment
metadata:
  name: store-admin
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.storeAdmin.replicas }}
  selector:
    matchLabels:
      app: store-admin
  template:
    metadata:
      labels:
        app: store-admin
    spec:
      containers:
        - name: store-admin
          image: {{ .Values.image.registry }}/bestbuy-store-admin:{{ .Values.image.tag }}
          ports:
            - containerPort: {{ .Values.storeAdmin.port }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.storeAdmin.port }}
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: store-admin
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: store-admin
  ports:
    - port: {{ .Values.storeAdmin.port }}
      targetPort: {{ .Values.storeAdmin.port }}
  type: LoadBalancer
